{# this is the form component #}

{{ form_start(form) }}
<div class="col-md-5">
    {{ form_widget(form) }}
</div>
<button class="btn btn-primary">{{ button | default('Enregistrer') }}</button>{# default label for button #}
{{ form_end(form) }}