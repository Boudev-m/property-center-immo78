{% extends 'base.html.twig' %}

{% block title %}Inscription - Immo78{% endblock %}

{% block body %}

<div class="container my-5 col-6 text-center" style="max-width: 1020px;">
    <p class="fs-4 text-center">Créer un autre compte administrateur</p>
    <div class="mx-auto" style="max-width: 300px;">

        {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.username) }}
        {{ form_row(registrationForm.plainPassword) }}
        <button type="submit" class="btn btn-light rounded-0">Valider</button>
        {{ form_end(registrationForm) }}
    </div>

    <!-- <form action=" {{ path('admin.register') }}" method="post">
        <div class="form-group">
            <label for="username">Choisissez un nom</label>
            <input type="text" id="username" name="_username" class="form-control">
        </div>
        <div class="form-group">
            <label for="password">Choisissez un mot de passe</label>
            <input type="password" id="password" name="_password" class="form-control">
        </div>

        <button type="submit" class="btn btn-secondary">S'inscrire</button>
    </form> -->

</div>
{% endblock %}